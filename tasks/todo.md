# 100챌린지 — TODO

> 프로젝트 실시간 진행 상태. 세션 시작 시 반드시 이 파일부터 읽을 것.

---

## 이번 세션 (2026-07-24) — 속도 개선 + 다크 프리미엄 디자인 리뉴얼

### 속도 개선
- [x] 진입 시 로딩 제거: 캘린더/랭킹 localStorage 캐시 → 즉시 렌더 후 백그라운드 갱신 (stale-while-revalidate)
- [x] 달력 클릭 낙관적 업데이트: 화면 즉시 반영 → 서버 전송은 백그라운드, 실패 시 자동 롤백
- [x] `/api/available-months` 호출 제거 (월 목록 클라이언트 생성, 백엔드 엔드포인트는 유지)
- [x] 진입 시 랭킹 중복 fetch 제거 + 월 변경은 onMonthChange가 병렬 처리
- [x] 백엔드 `/api/calendar`: calculate_penalty의 중복 DB 쿼리 제거 (조회한 records 재사용)
- [x] Inter 웹폰트 로드 제거 (Pretendard만 사용)

### 디자인 리뉴얼 (GitHub 71K★ awesome-design-md의 Binance DESIGN.md 기반)
- [x] "Yellow Voltage on Deep Black": 캔버스 #0B0E11 + 카드 #1E2329 + 옐로우 #FCD535 단일 액센트
- [x] 플랫 컬러블록 (그림자/그라데이션 글로우 제거, 헤어라인 구분), 숫자 tabular-nums
- [x] 달력: 완료=옐로우+블랙 잉크, 오늘=옐로우 링, 미완료=레드 틴트, 일=빨강/토=파랑
- [x] 벌금 히어로: 옐로우 대형 숫자 (stat-callout 시그니처)
- [x] 1위 골드 이펙트 → 옐로우 브랜드 톤으로 통일 (기존 B 항목 색상충돌 해소)
- [x] PWA 아이콘 재생성(딥블랙+옐로우), manifest/theme-color #0B0E11, 상태바 black-translucent
- [x] 디자인 기준 파일 `DESIGN.md` 프로젝트 루트에 저장 (이 프로젝트 전용)
- [x] 서비스워커 캐시 v16 → v17
- [x] 검증: puppeteer-core+시스템 크롬 실스크린샷 (로그인/메인/달력/랭킹), 낙관적 업데이트 서버응답 전 반영 확인, 벌금 계산 정확
- [x] 커밋 + 푸시 → Render 자동 배포

---

## 이번 세션 (2026-04-24) — 완료

- [x] 백엔드: `detect_market()` + Yahoo Finance로 KR 주식 실시간 시세
- [x] `StockHolding.current_price`, `name` 컬럼 추가 + ALTER TABLE 마이그레이션
- [x] 관리자 폼: 시장 선택(나스닥/코스피/코스닥) + 구매단가 + 심볼 검증
- [x] `/api/assets`: 종목별/전체 수익률, 총투자금, 통화/시장 정보 확장
- [x] 자산 요약 카드: 화이트 디자인 + 평가금액 + 수익률 + 총투자금 + 현금보유
- [x] 종목 리스트: 한글 회사명(네이버 API), 내평균, 수익률 뱃지
- [x] 전체 UI 테마: 보라색 → 카카오톡 옐로우(#FEE500) + 화이트
- [x] 옐로우 배경 위 텍스트 검정(#191600) 가독성 확보
- [x] Pretendard Variable 폰트, PWA 아이콘 옐로우 재생성
- [x] 서비스워커 캐시 v14 → v16
- [x] 커밋 62c4d58, 65ae227 푸시 → Render 자동 배포

---

## 미완료 / 다음 세션 우선순위

### [A] 프로덕션 검증 (우선)
- [ ] Render 배포 로그 확인: `ALTER TABLE stock_holdings ADD COLUMN name VARCHAR(100)`이 Supabase에서 성공했는지 확인 (실패 시 try/except로 롤백되어 조용히 넘어감)
- [ ] `/api/assets` 첫 호출 시 기존 보유 종목들 `name` 컬럼이 레이지 백필되는지 확인 (관리자 로그인 후 자산 카드 확인)
- [ ] Finnhub API 키가 프로덕션에 설정돼 있는지 확인 — 없으면 US 종목 회사명이 안 나옴
- [ ] 모바일 PWA 새 아이콘(카카오옐로우) 정상 반영되는지 확인 (재설치 or 캐시 초기화 필요할 수 있음)

### [B] UI 추가 검토
- [ ] 명예의 전당 1위 골드 효과와 메인 카카오옐로우 테마의 색상 충돌 여부 — 구분력이 떨어질 수 있음. 1위 효과를 다른 색(예: 오렌지/로즈골드)으로 조정 검토
- [ ] 헤더 아이콘(44x44 옐로우 박스)이 화이트 헤더에서 적절히 대비되는지 실기기 확인
- [ ] PWA 설치 배너 디자인 실기기 확인

### [C] 기능 아이디어 (백로그)
- [ ] KR 주식도 일간 변동률(change_percent) 뱃지 표시 (현재는 계산된 누적 수익률만 표시)
- [ ] 환율 변동 그래프 미니 카드
- [ ] 종목 편집 UI: 현재 prompt 기반 → 모달로 개선
- [ ] 회사명 백필 실패 시 관리자가 수동 입력할 수 있는 필드

### [D] 리팩토링 (선택)
- [ ] 3500+줄 `templates/index.html` 분리 검토 (CSS/JS 별도 파일)
- [ ] `app.py` 라우트별 모듈 분리

---

## 다음 세션 시작 시 체크리스트

1. `memory/LATEST_CONTEXT.md` 읽기 (프로젝트 현재 상태 전체)
2. 이 파일(`tasks/todo.md`) 읽기 (진행 중인 작업)
3. `memory/sessions/2026-04-24_kr-stocks-kakao-yellow.md` 참고 (이번 세션 상세)
4. `[A] 프로덕션 검증` 항목부터 관리자에게 확인 요청
